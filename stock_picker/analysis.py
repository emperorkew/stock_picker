"""Screening and stock-picking logic."""

import pandas as pd


def pick_stocks(df: pd.DataFrame) -> pd.DataFrame:
    """Apply screening rules and return the selected stocks.

    Placeholder: returns the input unchanged until real rules are added.
    """
    return df